<Application x:Class="Hypixel_Skyblock_shop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Hypixel_Skyblock_shop"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <FontFamily x:Key="Monocraft">./Fonts/Monocraft.ttf#Monocraft</FontFamily>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource Monocraft}"></Setter>
            <Setter Property="Padding" Value="5"></Setter>
            <Setter Property="BorderThickness" Value="0"></Setter>
            <Setter Property="Margin" Value="5"></Setter>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="{StaticResource Monocraft}"></Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource Monocraft}"></Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource Monocraft}"></Setter>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="FontFamily" Value="{StaticResource Monocraft}"></Setter>
        </Style>

        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="{StaticResource Monocraft}"></Setter>
        </Style>
    </Application.Resources>
</Application>
